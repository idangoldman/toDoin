define('ToDoin', function() {
        return Backbone.View.extend({
            initialize: function() {
                // $('body').prepend(this.render().el);
            },
            attributes: function() {
                // return {
                //     'id': 'application'
                // };
            },
            render: function() {
                // var elements = [Header, Bowl, Salads],
                //     that = this;

                // that.$el.empty();

                // _.each(elements, function(element, index) {
                //     that.$el.append(( new element() ).$el.html());
                // });

                // return that;
            }
        });
    }
);